п»їnamespace Algorithm
{
    public class opz
    {
        //string[] elements = "25 8 70 * + 6 2 / -".Split(' ');

        //С‚СѓС‚ x - СЌС‚Рѕ РїРµСЂРµРјРµРЅРЅР°СЏ 
        string[] elements = "25 x 70 * + 6 2 / -".Split(' ');

        Stack<double> stack = new Stack<double>();
        void calc()
        {
            /*double v;
            for (int i = 0; i < elements.Length; i++)
            {
                if (double.TryParse(elements[i], out v))
                {
                    stack.Push(v);
                }
                else
                {
                    double a1 = stack.Pop(); // TODO РїСЂРѕРІРµСЂРёС‚СЊ РїРѕСЂСЏРґРѕРє
                    double a2 = stack.Pop();
                    stack.Push(a1 elements[i] a2);
                }
            }

            bool calc(x, out y)
            {
                y = calc;
                return trycalc
            }*/
        }
    }
}
